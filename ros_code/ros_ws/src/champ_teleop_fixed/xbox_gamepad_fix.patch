diff --git a/champ_teleop.py b/champ_teleop.py
index af45450..1ee1cba 100755
--- a/champ_teleop.py
+++ b/champ_teleop.py
@@ -3,6 +3,7 @@
 
 from __future__ import print_function
 
+import math
 import select
 import sys
 import termios
@@ -52,12 +53,14 @@ class Teleop(Node):
         self.declare_parameter("gait/nominal_height", 0)
         self.declare_parameter("speed", 0.5)
         self.declare_parameter("turn", 1.0)
+        self.declare_parameter("use_joy", False)
         
         self.swing_height = self.get_parameter("gait/swing_height").value
         self.nominal_height = self.get_parameter("gait/nominal_height").value
 
         self.speed = self.get_parameter("speed").value
         self.turn = self.get_parameter("turn").value
+        self.use_joy = self.get_parameter("use_joy").value
 
         self.msg = """
 Reading from the keyboard  and Publishing to Twist!
@@ -119,7 +122,9 @@ CTRL-C to quit
                 'c':(1,.9),
             }
         
-        self.poll_keys()
+        # Only start keyboard polling if not using joystick
+        if not self.use_joy:
+            self.poll_keys()
 
     def joy_callback(self, data):
         twist = Twist()
@@ -240,3 +245,16 @@ CTRL-C to quit
 if __name__ == "__main__":
     rclpy.init()
     teleop = Teleop()
+    
+    # If using joystick mode, keep the node spinning to process joy callbacks
+    if teleop.use_joy:
+        try:
+            rclpy.spin(teleop)
+        except KeyboardInterrupt:
+            pass
+        finally:
+            teleop.destroy_node()
+            rclpy.shutdown()
+    # If using keyboard mode, poll_keys() handles the main loop and shutdown
+    else:
+        rclpy.shutdown()
diff --git a/launch/teleop.launch.py b/launch/teleop.launch.py
index 9707272..f6e4e16 100644
--- a/launch/teleop.launch.py
+++ b/launch/teleop.launch.py
@@ -42,7 +42,7 @@ def generate_launch_description():
         emulate_tty=True,
         parameters=[
             {'use_sim_time': use_sim_time},
-            {"joy": use_joy}
+            {"use_joy": use_joy}
             ]
     )
     return LaunchDescription(
